server {
    listen 80;
    listen [::]:80;
    server_name {{DOMAIN}} file.{{DOMAIN}};

    location /.well-known/acme-challenge/ {
        root {{WEBROOT}};
    }

    # Temporary ACME server
    location / {
        return 200 "ACME challenge server ready";
    }
}
