<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{DOMAIN}} - Welcome</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
        }
        
        .container {
            background: white;
            border-radius: 10px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            padding: 50px;
            max-width: 600px;
            text-align: center;
        }
        
        h1 {
            color: #667eea;
            margin-bottom: 20px;
            font-size: 2.5em;
        }
        
        p {
            color: #666;
            line-height: 1.6;
            margin-bottom: 15px;
            font-size: 1.1em;
        }
        
        .status {
            background: #f0f4ff;
            border-left: 4px solid #667eea;
            padding: 15px;
            text-align: left;
            margin: 30px 0;
            border-radius: 5px;
            font-family: 'Courier New', monospace;
            font-size: 0.95em;
        }
        
        .status-item {
            margin: 10px 0;
        }
        
        .status-item .label {
            color: #667eea;
            font-weight: bold;
        }
        
        .footer {
            color: #999;
            font-size: 0.9em;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to {{DOMAIN}}</h1>
        <p>Your server is up and running.</p>
        
        <div class="status">
            <div class="status-item">
                <span class="label">Domain:</span> {{DOMAIN}}
            </div>
            <div class="status-item">
                <span class="label">Protocol:</span> HTTPS / HTTP/3
            </div>
            <div class="status-item">
                <span class="label">Status:</span> ✓ Online
            </div>
        </div>
        
        <p>This is a placeholder page. Configure your services to serve content on this domain.</p>
        
        <div class="footer">
            <p>Generated at server startup</p>
        </div>
    </div>
</body>
</html>
